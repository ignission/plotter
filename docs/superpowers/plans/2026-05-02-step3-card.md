# Step 3: カード生成（utils + card + parts）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PLOTTER の最小単位「カード」（角丸矩形を厚み extrude した板）を生成する。標準カード（30×30×2mm）、ワイドカード（60×30×2mm）、厚み試作用バッチ（1.5/2/2.5/3mm の4枚を一括）の3エントリポイントから STL/STEP を出力できるようにする。

**Architecture:**
- `src/plotter/utils.py` に汎用ヘルパー `make_rounded_plate(width, height, thickness, corner_radius) -> Part` を実装。今後 body / base でも再利用される
- `src/plotter/card.py` に `make_standard_card(p)` / `make_wide_card(p)` を実装。引数 `p: Params` を取り、デフォルトはモジュール `params` シングルトン → ハードコード回避と試作時のオーバーライドを両立
- `parts/card_standard.py` / `parts/card_wide.py` / `parts/card_thickness_test.py` がエントリポイント。`build/` に STL/STEP を出力
- 既存 `Makefile` の `make card` ターゲットに `card_thickness_test.py` を追加

**Tech Stack:** build123d 0.10.0、pytest、Python 3.13

---

## File Structure

- Create: `src/plotter/utils.py` — `make_rounded_plate()` 1関数のみ（責務単一）
- Create: `src/plotter/card.py` — `make_standard_card()` / `make_wide_card()` の2関数
- Create: `tests/test_utils.py` — `make_rounded_plate` のテスト
- Create: `tests/test_card.py` — カード関数のテスト
- Create: `parts/__init__.py` — パッケージマーカー（空）
- Create: `parts/card_standard.py` — 標準カードのSTL/STEP出力
- Create: `parts/card_wide.py` — ワイドカードのSTL/STEP出力
- Create: `parts/card_thickness_test.py` — 厚み 1.5/2/2.5/3mm の4枚を1ファイルに出力（試作専用）
- Modify: `Makefile` — `card:` ターゲットに `card_thickness_test.py` を追加

`parts/` は STL 生成スクリプトの集合。pytest 対象外（`testpaths = ["tests"]`）。

---

## Task 1: utils.make_rounded_plate を TDD で実装

**Files:**
- Create: `tests/test_utils.py`
- Create: `src/plotter/utils.py`

- [ ] **Step 1: tests/test_utils.py を書く**

```python
"""plotter.utils の汎用ヘルパー関数テスト。"""

import pytest
from build123d import Part

from plotter.utils import make_rounded_plate


def test_make_rounded_plate_returns_part():
    """戻り値は build123d の Part（BuildPart().part の型）。"""
    plate = make_rounded_plate(30.0, 30.0, 2.0, 3.0)
    assert isinstance(plate, Part)


def test_make_rounded_plate_bbox_matches_dimensions():
    """bounding box が指定寸法と一致すること。"""
    plate = make_rounded_plate(30.0, 30.0, 2.0, 3.0)
    bbox = plate.bounding_box()
    assert bbox.size.X == pytest.approx(30.0, abs=0.01)
    assert bbox.size.Y == pytest.approx(30.0, abs=0.01)
    assert bbox.size.Z == pytest.approx(2.0, abs=0.01)


def test_make_rounded_plate_corner_is_rounded():
    """角R が効いている = 矩形より体積が小さく、削れすぎてもいないこと。"""
    plate = make_rounded_plate(30.0, 30.0, 2.0, 3.0)
    rect_volume = 30.0 * 30.0 * 2.0
    # 角R による削れ: (4 - π) * r² * thickness ≈ 0.86 * 9 * 2 ≈ 15.5mm³
    assert plate.volume < rect_volume
    assert plate.volume > rect_volume * 0.95  # 5%以上は残る


def test_make_rounded_plate_zero_radius_equals_box():
    """corner_radius=0 なら矩形ボックスと同体積（許容誤差内）。

    RectangleRounded は radius=0 で OCC 例外を出すため、関数内で
    Rectangle に分岐する必要がある（codex review で検証済み）。
    """
    plate = make_rounded_plate(30.0, 30.0, 2.0, 0.0)
    rect_volume = 30.0 * 30.0 * 2.0
    assert plate.volume == pytest.approx(rect_volume, rel=1e-6)


def test_make_rounded_plate_wide_dimensions():
    """ワイドカード相当 60×30×2 でも bbox が一致。"""
    plate = make_rounded_plate(60.0, 30.0, 2.0, 3.0)
    bbox = plate.bounding_box()
    assert bbox.size.X == pytest.approx(60.0, abs=0.01)
    assert bbox.size.Y == pytest.approx(30.0, abs=0.01)
    assert bbox.size.Z == pytest.approx(2.0, abs=0.01)
```

- [ ] **Step 2: テスト実行 → ImportError で FAIL（RED）**

Run: `mise exec -- uv run pytest tests/test_utils.py -v`
Expected: ModuleNotFoundError

- [ ] **Step 3: src/plotter/utils.py を実装**

```python
"""build123d 共通ヘルパー関数。

各部品（card / body / base）から再利用される汎用形状ユーティリティを集約。
"""

from build123d import BuildPart, BuildSketch, Part, Rectangle, RectangleRounded, extrude


def make_rounded_plate(
    width: float,
    height: float,
    thickness: float,
    corner_radius: float,
) -> Part:
    """角丸矩形を厚み extrude した板状パーツを返す。

    原点はパーツ中心 (X=0, Y=0)、底面は Z=0、上面は Z=thickness。
    corner_radius=0 では Rectangle にフォールバックする
    （RectangleRounded は OCC で半径0を許容しないため）。
    """
    with BuildPart() as part:
        with BuildSketch():
            if corner_radius > 0:
                RectangleRounded(width, height, corner_radius)
            else:
                Rectangle(width, height)
        extrude(amount=thickness)
    return part.part
```

- [ ] **Step 4: テスト実行 → 全 PASS**

Run: `mise exec -- uv run pytest tests/test_utils.py -v`
Expected: 5 passed

- [ ] **Step 5: 既存テストも回帰なしを確認**

Run: `mise exec -- uv run pytest -v`
Expected: 18 passed (params 7 + clearance 6 + utils 5)

- [ ] **Step 6: ruff format & check**

Run: `mise exec -- uv run ruff format src/ tests/ && mise exec -- uv run ruff check src/ tests/`
Expected: 0 エラー

- [ ] **Step 7: コミット**

```bash
git add src/plotter/utils.py tests/test_utils.py
git commit -m "$(cat <<'EOF'
角丸板ヘルパー make_rounded_plate を utils に追加

card / body / base から再利用する汎用形状関数。RectangleRounded を
extrude するだけのシンプルなラッパー。corner_radius=0 で通常矩形と
等価。
EOF
)"
```

---

## Task 2: card.make_standard_card / make_wide_card を TDD で実装

**Files:**
- Create: `tests/test_card.py`
- Create: `src/plotter/card.py`

- [ ] **Step 1: tests/test_card.py を書く**

```python
"""カード生成関数のテスト。"""

from dataclasses import replace

import pytest

from plotter.card import make_standard_card, make_wide_card
from plotter.params import params


def test_standard_card_bbox_matches_params():
    """標準カードの bbox が params 既定値と一致すること。"""
    card = make_standard_card()
    bbox = card.bounding_box()
    assert bbox.size.X == pytest.approx(params.card_width_std, abs=0.01)
    assert bbox.size.Y == pytest.approx(params.card_height, abs=0.01)
    assert bbox.size.Z == pytest.approx(params.card_thickness, abs=0.01)


def test_wide_card_bbox_matches_params():
    """ワイドカードの bbox が params 既定値と一致すること。"""
    card = make_wide_card()
    bbox = card.bounding_box()
    assert bbox.size.X == pytest.approx(params.card_width_wide, abs=0.01)
    assert bbox.size.Y == pytest.approx(params.card_height, abs=0.01)
    assert bbox.size.Z == pytest.approx(params.card_thickness, abs=0.01)


def test_card_thickness_override_via_custom_params():
    """custom Params(card_thickness=...) で厚みをオーバーライドできること。"""
    custom = replace(params, card_thickness=3.0)
    card = make_standard_card(custom)
    assert card.bounding_box().size.Z == pytest.approx(3.0, abs=0.01)


def test_card_corner_is_rounded():
    """角R が効いている = 矩形より体積が小さい。"""
    card = make_standard_card()
    rect_volume = params.card_width_std * params.card_height * params.card_thickness
    assert card.volume < rect_volume
    assert card.volume > rect_volume * 0.95


def test_wide_card_volume_is_slightly_over_double_standard():
    """ワイド (60mm) は標準 (30mm) の2倍をわずかに超える体積。

    角Rの削れは標準・ワイドで同じ量（同じ4角）なので、
    std_volume = 1800 - cut, wide_volume = 3600 - cut
    → ratio = (3600 - cut) / (1800 - cut) > 2 になる（cut > 0 のため）。
    cut ≈ 15.5mm³ なので ratio ≈ 3584.5 / 1784.5 ≈ 2.009。
    """
    std = make_standard_card()
    wide = make_wide_card()
    ratio = wide.volume / std.volume
    assert 2.0 < ratio < 2.02
```

- [ ] **Step 2: テスト実行 → ImportError で FAIL**

Run: `mise exec -- uv run pytest tests/test_card.py -v`
Expected: ModuleNotFoundError

- [ ] **Step 3: src/plotter/card.py を実装**

```python
"""カード（標準・ワイド）の形状生成関数。"""

from build123d import Part

from plotter.params import Params, params as default_params
from plotter.utils import make_rounded_plate


def make_standard_card(p: Params = default_params) -> Part:
    """標準カード（card_width_std × card_height × card_thickness）を生成する。"""
    return make_rounded_plate(
        width=p.card_width_std,
        height=p.card_height,
        thickness=p.card_thickness,
        corner_radius=p.card_corner_radius,
    )


def make_wide_card(p: Params = default_params) -> Part:
    """ワイドカード（card_width_wide × card_height × card_thickness）を生成する。"""
    return make_rounded_plate(
        width=p.card_width_wide,
        height=p.card_height,
        thickness=p.card_thickness,
        corner_radius=p.card_corner_radius,
    )
```

- [ ] **Step 4: テスト実行 → 全 PASS**

Run: `mise exec -- uv run pytest tests/test_card.py -v`
Expected: 5 passed

- [ ] **Step 5: 全テスト確認**

Run: `mise exec -- uv run pytest -v`
Expected: 23 passed

- [ ] **Step 6: ruff**

Run: `mise exec -- uv run ruff format src/ tests/ && mise exec -- uv run ruff check src/ tests/`
Expected: 0 エラー

- [ ] **Step 7: コミット**

```bash
git add src/plotter/card.py tests/test_card.py
git commit -m "$(cat <<'EOF'
カード生成関数 make_standard_card / make_wide_card を追加

params から card_width_std=30 / card_width_wide=60 / card_height=30 /
card_thickness=2 / card_corner_radius=3 を参照して角丸板を生成する。
custom Params を渡せば厚み等のオーバーライドが可能（試作用）。
EOF
)"
```

---

## Task 3: parts/ エントリポイント3つを追加

**Files:**
- Create: `parts/card_standard.py`
- Create: `parts/card_wide.py`
- Create: `parts/card_thickness_test.py`

`parts/__init__.py` は不要（スクリプト実行のみで import されない）。

- [ ] **Step 2: parts/card_standard.py を実装**

```python
"""標準カード(30×30×2mm)のSTL/STEP出力エントリ。

Usage:
    mise exec -- uv run python parts/card_standard.py
"""

from pathlib import Path

from build123d import export_step, export_stl

from plotter.card import make_standard_card


def main() -> None:
    card = make_standard_card()
    out_dir = Path(__file__).resolve().parent.parent / "build"
    out_dir.mkdir(parents=True, exist_ok=True)
    export_stl(card, str(out_dir / "card_standard.stl"))
    export_step(card, str(out_dir / "card_standard.step"))
    print(f"Wrote: {out_dir}/card_standard.stl")
    print(f"Wrote: {out_dir}/card_standard.step")


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: parts/card_wide.py を実装**

```python
"""ワイドカード(60×30×2mm)のSTL/STEP出力エントリ。

Usage:
    mise exec -- uv run python parts/card_wide.py
"""

from pathlib import Path

from build123d import export_step, export_stl

from plotter.card import make_wide_card


def main() -> None:
    card = make_wide_card()
    out_dir = Path(__file__).resolve().parent.parent / "build"
    out_dir.mkdir(parents=True, exist_ok=True)
    export_stl(card, str(out_dir / "card_wide.stl"))
    export_step(card, str(out_dir / "card_wide.step"))
    print(f"Wrote: {out_dir}/card_wide.stl")
    print(f"Wrote: {out_dir}/card_wide.step")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: parts/card_thickness_test.py を実装**

CLAUDE.md の「厚み 1.5 / 2 / 2.5 / 3mm を一度に印刷（パラメータを変えて4ファイル生成）」に
従い、4つの STL/STEP ファイルを別々に出力する。スライサ側で4ファイル全部を読み込めば
1回の印刷で厚み比較ができる。ファイル名で厚みが識別できるので印刷後の混同もない。

```python
"""カードの厚み試作（1.5/2/2.5/3mm）を4ファイルに分割出力する試作専用エントリ。

CLAUDE.md の指示通り、各厚みごとに独立した STL/STEP を生成する。
スライサで4ファイルをまとめて読み込めば1回の印刷で比較できる。
ファイル名は card_standard_t{thickness}.stl の形式。

Usage:
    mise exec -- uv run python parts/card_thickness_test.py
"""

from dataclasses import replace
from pathlib import Path

from build123d import export_step, export_stl

from plotter.card import make_standard_card
from plotter.params import params

THICKNESSES: list[float] = [1.5, 2.0, 2.5, 3.0]


def main() -> None:
    out_dir = Path(__file__).resolve().parent.parent / "build"
    out_dir.mkdir(parents=True, exist_ok=True)
    for t in THICKNESSES:
        card = make_standard_card(replace(params, card_thickness=t))
        stem = f"card_standard_t{t}"
        export_stl(card, str(out_dir / f"{stem}.stl"))
        export_step(card, str(out_dir / f"{stem}.step"))
        print(f"Wrote: {out_dir}/{stem}.stl ({t}mm)")
        print(f"Wrote: {out_dir}/{stem}.step ({t}mm)")


if __name__ == "__main__":
    main()
```

- [ ] **Step 5: 3スクリプトを実行して STL/STEP が出力されること**

```bash
mise exec -- uv run python parts/card_standard.py
mise exec -- uv run python parts/card_wide.py
mise exec -- uv run python parts/card_thickness_test.py
ls -la build/card_*.stl build/card_*.step
```

Expected: 12 ファイル
- card_standard.{stl,step}（2）
- card_wide.{stl,step}（2）
- card_standard_t1.5/t2.0/t2.5/t3.0.{stl,step}（8）
すべてサイズ>0

- [ ] **Step 6: ruff format & check**

Run: `mise exec -- uv run ruff format parts/ && mise exec -- uv run ruff check parts/`
Expected: 0 エラー

- [ ] **Step 7: コミット**

```bash
git add parts/card_standard.py parts/card_wide.py parts/card_thickness_test.py
git commit -m "$(cat <<'EOF'
カード STL 出力エントリポイントを追加

- parts/card_standard.py — 標準カード単品
- parts/card_wide.py — ワイドカード単品
- parts/card_thickness_test.py — 厚み 1.5/2/2.5/3mm の4ファイルを別々に出力。
  スライサで4ファイル読み込めば1回の印刷で比較可能。CLAUDE.md の試作プラン
  「4ファイル生成」に準拠。
EOF
)"
```

---

## Task 4: Makefile に card_thickness_test を追加

**Files:**
- Modify: `Makefile`

- [ ] **Step 1: Makefile を読む**

- [ ] **Step 2: `card:` ターゲットに `card_thickness_test.py` の実行を追加**

現状:
```makefile
card: $(BUILD_DIR)
	$(PYTHON) parts/card_standard.py
	$(PYTHON) parts/card_wide.py
```

変更後:
```makefile
card: $(BUILD_DIR)
	$(PYTHON) parts/card_standard.py
	$(PYTHON) parts/card_wide.py
	$(PYTHON) parts/card_thickness_test.py
```

`help:` ブロックの該当行を `make card - Build all card variants (standard / wide / thickness test)` に更新。

- [ ] **Step 3: 動作確認**

Run: `make clean && make card && ls build/card_*`
Expected: 12 ファイル出力（standard / wide / 厚み4種 × .stl/.step）

- [ ] **Step 4: コミット**

```bash
git add Makefile
git commit -m "make card に card_thickness_test を追加"
```

---

## 完了の定義

- `mise exec -- uv run pytest -v` で **23 passed** (params 7 + clearance 6 + utils 5 + card 5)
- `make card` で 12 ファイル（card_standard / card_wide / 厚み4種 × .stl/.step）が `build/` に生成
- `make clearance` で `tenon_clearance_test.{stl,step}` が生成
- `mise exec -- uv run ruff check src/ tests/ parts/` 0 エラー
- 4コミットが main にある

## 次フェーズへの引き継ぎ

- カード厚み試作を1.5/2/2.5/3mm で印刷 → 触り心地・反り・読みやすさで最適厚みを決める
- 結果を `params.card_thickness` に反映
- Step 4（本体パネル）に進む

---

## Self-Review

**Spec coverage（CLAUDE.md と照合）:**
- ✅ src/plotter/utils.py 角丸スケッチ汎用関数
- ✅ src/plotter/card.py カード生成関数
- ✅ tests/test_card.py 寸法テスト
- ✅ parts/card_standard.py STL出力エントリ
- ✅ parts/card_wide.py STL出力エントリ
- ✅ 厚み 1.5/2/2.5/3mm の試作（card_thickness_test.py）
- ✅ params.* からの参照（ハードコード回避）

**Placeholder scan:** TODO/TBD なし。全コード完全実装。

**Type consistency:**
- `make_rounded_plate(width, height, thickness, corner_radius) -> Part`
- `make_standard_card(p: Params = default_params) -> Part`
- `make_wide_card(p: Params = default_params) -> Part`
- テストも一致。

**Risk:**
- build123d 0.10.0 で `RectangleRounded(width, height, radius)` の引数並びが変わっている可能性。実装サブエージェントに最新 API を確認させ、mismatch なら DONE_WITH_CONCERNS で報告。
- `make_rounded_plate` の戻り値が `Part` か `Compound` か（Step 2 同様に build123d API 依存）→ テストでは `Part | Compound` 両対応にしてある。
